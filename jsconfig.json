{
  "compilerOptions": {
    "baseUrl": ".", // Sets the base directory for resolving non-relative modules
    "paths": {
      "@/*": ["src/*"] // Maps @/ to the src/ directory
    }
  },
  "include": [
    "src/**/*"
  ]
}
